# Task Flow 2.1.1 新对话交接

## 项目目录

项目目录：`D:\codex\task_flow2.1.1\V2.1`

只在该目录内工作。

不要修改 `task-flow_back`，它只是恢复参考。

## 新对话先读

必须先读：

- `README.md`
- `doc/V2.1.1/Task Flow 2.1.1 新对话交接.md`

归档目录里的旧文档默认不要读，只有追溯旧决策或排查回归时再读。

## 当前状态

全局任务面板升级已经完成，并归档。

标签分组功能升级已经完成，并通过自动测试和用户手动验证。

标签功能说明保留在：

- `doc/V2.1.1/Task Flow 2.1.1 标签分组功能升级方案.md`
- `doc/V2.1.1/Task Flow 2.1.1 标签分组功能开发计划.md`

当前进入维护状态。

后续只处理用户明确提出的问题或小细节优化，不要主动开启新大功能。

## 当前产物

当前可用构建产物：

`D:\codex\task_flow2.1.1\V2.1\dist\task-flow`

最近一次构建时间：

- `main.js`：`2026/6/21 18:02:14`
- `styles.css`：`2026/6/21 17:47:46`

构建和测试均通过：

```powershell
npm.cmd run build
npm.cmd test
```

## 关键提醒

- 插件不要求当前打开文档必须是月文档。
- 任务仍然写入对应目标月文档，不写入当前外部文档。
- 第一版不自动创建月文档、周区域、日期区域。
- 标签使用 Obsidian 原生 `#标签` 语法，Markdown 中仍保存为任务名称在前、标签在后。
- 当前版本不做标签筛选条。

## 冻结范围

- 041 的手机端布局、创建页、底部操作栏、渐变、滚动隔离、未完成任务面板不要无故改动。
- 不要恢复任务名称 Markdown / Live Preview 输入方案。
- 不要恢复 `titleMd`。
- 不要恢复任务行 Markdown 渲染。
- 除已完成的标签 `tags` 可选字段外，不要为了输入方式继续修改任务数据模型、文档任务块格式或任务行布局。

## 后续开发原则

- 涉及手机端 UI、输入框、任务行显示、底部栏、滚动隔离时，必须先说明影响范围。
- 小改动先做最小闭环，不做跨模块重构。
- 修改后至少运行：
  - `npm.cmd run build`
  - `npm.cmd test`
